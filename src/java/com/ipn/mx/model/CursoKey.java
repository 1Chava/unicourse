/*
 * This java source file is generated by DAO4J v1.19
 * Generated on Tue May 01 20:32:32 CDT 2018
 * For more information, please contact b-i-d@163.com
 * Please check http://sourceforge.net/projects/dao4j/ for the latest version.
 */

package com.ipn.mx.model;

public class CursoKey implements java.io.Serializable, Cloneable {
    /* idCurso */
    protected int idcurso;

    /* Usuario_idUsuarios */
    protected int usuarioIdusuarios;

    /* idCurso */
    public int getIdcurso() {
        return idcurso;
    }

    /* idCurso */
    public void setIdcurso(int idcurso) {
        this.idcurso = idcurso;
    }

    /* Usuario_idUsuarios */
    public int getUsuarioIdusuarios() {
        return usuarioIdusuarios;
    }

    /* Usuario_idUsuarios */
    public void setUsuarioIdusuarios(int usuarioIdusuarios) {
        this.usuarioIdusuarios = usuarioIdusuarios;
    }

    /* Calculate hash code */
    public int hashCode() {
        int hashCode = 0;
        hashCode += new Integer(idcurso).hashCode();
        hashCode += new Integer(usuarioIdusuarios).hashCode();
        return hashCode;
    }
    
    /* Indicates whether some other object is "equal to" this one. */
    public boolean equals(Object obj) {
        if (this == obj)
            return true;

        if (obj == null || !(obj instanceof CursoKey))
            return false;

        CursoKey key = (CursoKey) obj;

        if (this.idcurso != key.idcurso)
            return false;

        if (this.usuarioIdusuarios != key.usuarioIdusuarios)
            return false;

        return true;
    }

    /* Creates and returns a copy of this object. */
    public Object clone()
    {
        CursoKey key = new CursoKey();
        key.idcurso = this.idcurso;
        key.usuarioIdusuarios = this.usuarioIdusuarios;
        return key;
    }

    /* Returns a string representation of the object. */
    public String toString() {
        String sep = "\r\n";
        StringBuffer sb = new StringBuffer();
        sb.append(this.getClass().getName()).append(sep);
        sb.append("[").append("idcurso").append(" = ").append(idcurso).append("]");
        sb.append("[").append("usuarioIdusuarios").append(" = ").append(usuarioIdusuarios).append("]");
        return sb.toString();
    }
}